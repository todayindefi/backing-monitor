{
  "schema_version": "contract-overlay/1",
  "producer": "riskanalyst",
  "asset": "susds",
  "as_of": "2026-09-07T00:00:00Z",
  "contract": {
    "structural_score": 7.0,
    "structural_score_source": "riskAnalyst:authored",
    "structural_score_generated_at": "2026-09-07T00:00:00Z",
    "structural_score_basis": "Axis 5 = CONTRACT & ADMIN, both halves. ⚠️ HELD at 7.0 on 2026-09-07 and now MEASURED rather than partly inferred: security_analyst's hand-walk replaced a v1.7 generator file that had ZERO walked layers. ⚠️ THE WALK RESOLVED FOUR UNKNOWNS IN OUR FAVOUR, WHICH IS WHY THIS IS A HOLD AND NOT A CUT. The generator file claimed contract-upgrade/ethereum was `timelock: none` and left base, arbitrum and optimism `unresolved`. All five layers measure 2d. The file was adverse-wrong on four of five rows — a reminder that generator output errs in both directions, not just the flattering one. ⚠️ AND EVERY ADVERSE FINDING WE HAD ALREADY PRICED IS CONFIRMED, none softened: (1) NO INDEPENDENT CANCELLER — verified source applies the same DSAuth `auth` modifier to both `plot()` and `drop()`, so the authority that proposes a plan is the authority that cancels it. Against a compromised proposer the cancel power belongs to the compromised party. **48 hours nobody independent can act on is 48 hours of NOTICE, not of PROTECTION.** (2) NO DELAY FLOOR — `MIN_DELAY()` and `MINIMUM_DELAY()` BOTH REVERT, neither appears in the verified ABI, and `setDelay(uint256)` executes through the pause proxy. **2 days is a current setting, not a demonstrated hard floor** — reducible by the same authority. A configured delay is not an enforced one. (3) THE SPBEAM BYPASS IS REAL — `set()` is available to a bud with no DSPause — BUT IT IS BOUNDED, and the bound is measured: cooldown `tau() = 57,600s (16h)`, `cfgs(\"DSR\") = (min 0, max 3000, step 400)` bps, facilitator a 2-of-3 Safe. An undelayed RATE-BOUND path, not a full-reach bypass. ⚠️ HELD AT 7.0 ON OUR OWN COHORT RUBRIC, NOT LENIENCY: what earns a dock is the ABSENCE OF A REACTION WINDOW. USDS still gives a holder one where the docked cases — USDT, USDC, thBILL, all zero delay — give none. The qualifications above narrow what the 48h buys; they do not remove it. ⚠️ USDS IS THE CONTROL CASE that rubric is pinned to, so this row is load-bearing beyond this asset.",
    "code_facts": [
      "Battle-tested MakerDAO-derived ERC-4626 / PSM / vault codebase — the lowest contract risk in the tokenized-yield set. [riskAnalyst assessment]",
      "⚠️ The binding concern is not code: USDS is upgradeable with a governance-addable freeze/blacklist. sUSDS is a wrapper over that surface and inherits it. [riskAnalyst assessment]",
      "⚠️ SPBEAM's `buds(address)` is a MAPPING and is not natively enumerable, so the bud count is bounded-below, NOT established. A dead-address control returned not-a-bud, so the probe is sound rather than silently matching everything — the count is unverified, not one. [security_analyst 2026-09-07]",
      "⚠️ Audits do not offset an authority path. The code half is recorded and is NOT credited against the authority half. [riskAnalyst assessment]"
    ],
    "authority_note": "⚠️ The authority half is security_analyst's hand-walk (2026-09-07, all 5 layers walked_by: hand) and is rendered from THEIR file, not duplicated here. This overlay supplies the code half and the score. Their unmeasured[] carries 2 entries — rollup-native message finality is outside an administrative-delay observation, and DSPause's DSChief authority is not reduced to an off-chain signature threshold. Read them beside this: they bound what the walk establishes.",
    "cross_axis": [
      "⚠️ The GSM delay qualification was previously carried on the ISSUER axis. A delay's ENFORCEABILITY belongs here on Contract & Admin. Same fact, correct axis — do not price it twice.",
      "⚠️ sUSDS holds no reserve of its own: its collateral IS USDS's. Backing (7.0) is INHERITED from USDS and re-verified against it, not independently derived. A wrapper cannot outrank the asset it wraps on that asset's own book.",
      "⚠️ 'Endgame Stars/subDAO complexity' and the cross-chain PSM/bridge surface are PASSTHROUGH and belong on Dependencies (7.0), not here. Historically priced on this axis; flagged rather than moved."
    ]
  }
}
