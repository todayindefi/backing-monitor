{
 "schema_version": "contract-overlay/1",
 "producer": "riskanalyst",
 "asset": "usdat",
 "as_of": "2026-09-10T12:00:00Z",
 "contract": {
  "structural_score": 4.0,
  "structural_score_source": "riskAnalyst:authored",
  "structural_score_generated_at": "2026-09-10T00:00:00Z",
  "structural_score_basis": "⚠️⚠️ **CORRECTED 2026-09-10 — THIS BASIS SAID \"ONE KEY\" AND \"SINGLE-KEY FULL-REACH AUTHORITY\", AND THAT OVERSTATES IT.** backing-monitor caught it. **Saturn represents `0x610182581C93687Ca03F4a8E7f124f8cEC616820` as a Fireblocks 2-of-3 MPC wallet**, a disclosure that has been on the live dashboard since August and **is recorded in this repo's own susdat Issuer row** — I wrote \"one key\" in a fresh basis while my own file, two hundred lines away, carried the 2-of-3 claim. ⚠️ **On-chain reads CANNOT distinguish MPC from a single-key EOA** — no contract code and a low outbound nonce look identical either way — so `code_present: false, nonce 38` is exactly what MPC looks like too, and my reading was not evidence against the disclosure. **It remains ISSUER REPRESENTATION pending independent attestation**, carried at face value here as it is elsewhere in this repo. ✅ **THE FINDING SURVIVES THE CORRECTION INTACT AND THE SCORE DOES NOT MOVE.** A 2-of-3 quorum holding **both PROPOSER and CANCELLER** still means **no independent veto** — the cancel power sits with the same quorum that proposes — still **one organisation**, and still **a 5-day delay on a door only Saturn opens**, with all three shares internal to Saturn's own custody process. What changes is that the compromise threshold is **two shares of a quorum rather than one key**. **Read every \"single key\" framing above as \"a 2-of-3 quorum internal to one operator\".** ⚠️⚠️ **THE EXIT-VS-NOTICE QUESTION IS NOW RESOLVED AS GENUINELY UNRESOLVABLE FROM PUBLISHED SOURCES, AND BOTH CANDIDATE NUMBERS ARE WRONG FOR IT.** backing-monitor pointed at PegTracker's `trust_stack.vesting_period_days = 30`, rendered on the dashboard as an *\"ERC-4626 redemption queue\"*, against my measured 3 days — a factor of exactly ten, which is the signature of an order-of-magnitude slip rather than two different quantities. **Measured 2026-09-10:** ```\nvestingPeriod()        259,200 s = 3d    live on the vault\nMAX_VESTING_PERIOD()   7,776,000 s = 90d a CEILING, not the current value\n2,592,000 (30 days)    FOUND ON NEITHER CONTRACT across 18 candidate getters\n```\n⚠️ **The feed's 30 is a HARDCODED LITERAL** (`saturn_backing_analyzer.py:1856`, sitting beside `deposit_fee_bps: 10` and `min_withdrawal_usdat: 10`) — **not a measurement, and it matches no on-chain value.** ⚠️ **And my 3 days is not the exit path either:** `vestingPeriod` pairs with `lastDistributionTimestamp` and `MAX_VESTING_PERIOD`, which is the ERC-4626 yield-drip pattern, **not a redemption queue.** The WithdrawalQueue contract returned **no delay getter at all** across eighteen signatures, with a fabricated getter reverting as the control. **So the number that would make the 5-day notice meaningful is not published by either producer, and one surface is publishing a figure for it that has no source.** The notice period stays **UNRESOLVED, NOT FAVOURABLE** — and this is now a stronger statement than before, because the two figures that looked like answers have both been ruled out. ✅ **THE SHARED TERMINAL IS REAL AND IT IS THE STRONGEST DELAY IN THE STRC FAMILY — re-measured by riskAnalyst 2026-09-10, NOT adopted.** SaturnTimelock `0xfd5782e3bff366601da3973ae30c583de4f08a67` returns `getMinDelay()` = **432,000s = 5 days**, and `getThreshold()` REVERTS on it — so the object is a timelock and not a Safe, which is the discrimination, not the answer alone. It is **SELF-ADMINISTERED**: `hasRole(DEFAULT_ADMIN, itself)` is true while the proxy-admin holder and `0x…dEaD` are both false, so **shortening the delay or changing role membership must itself clear 5 days.** The proxy chain terminates properly — the USDat ProxyAdmin's `owner()` IS this timelock — and on **both** sUSDat `0xd166337499e176bbc38a1fbd113ab144e5bd2df7` and the WithdrawalQueue `0x4bc9fec04f0f95e9b42a3ef18f3c96fb57923d2e`, `hasRole(DEFAULT_ADMIN, timelock)` is true while `0x…dEaD` **and a FABRICATED role** are both false. ✅ **EXECUTION IS PERMISSIONLESS — `hasRole(EXECUTOR, address(0))` is TRUE**, so once an operation matures anyone can execute it and the operator cannot stall a queued action indefinitely. **There is NO zero-delay path anywhere on this surface**, which distinguishes it from apyUSD (three zero-notice powers) and thUSD (an undelayed full-reach bridge delegate). ⚠️⚠️ **AND THE KEY BEHIND THAT DELAY IS A SINGLE EOA THAT IS BOTH PROPOSER AND CANCELLER.** `0x610182581C93687Ca03F4a8E7f124f8cEC616820` — no code, nonce 38 — returns **PROPOSER true AND CANCELLER true**, with EXECUTOR and DEFAULT_ADMIN false, `0x…dEaD` false on all four and a fabricated role false. **So there is no independent veto: against a compromised proposer the cancel power belongs to the attacker.** One key can queue a full-reach upgrade of the token, the vault and the withdrawal queue; the only defence is that holders notice inside 5 days and leave. **This is a long delay guarding a one-key door.** ⚠️ **AND WHETHER 5 DAYS IS ACTUALLY EXIT-USABLE IS NOT ESTABLISHED.** `vestingPeriod()` on the vault reads 259,200s (3 days), but that is a yield-vesting parameter and **I did not resolve the withdrawal-queue delay from any getter** — a sweep of twelve candidate signatures returned nothing, with a fabricated getter reverting as the control. **A notice period is only protective if the exit is shorter than it, and that comparison is UNRESOLVED here, not favourable.** The queue's `totalSupply()` is 0, so nothing is currently pending. **WHY 4.0:** the 5-day self-administered delay with permissionless execution and no undelayed path argues genuinely up — it is a better-shaped surface than either asset scored today. **The single-EOA proposer-and-canceller argues hard down**, and single-key full-reach authority is the more serious of the two facts. **Anchored against this session: thUSD 4.0** (48h, but 38% of supply behind a Safe-mutable 18h and an undelayed full-reach delegate) **and apyUSD 4.5** (4-of-7 with a graduated ladder, but three zero-notice powers and a nominal Safe separation). **A single EOA holding both propose and cancel places this at thUSD's level, not apyUSD's, despite the longer delay.** ⚠️ **BNB — 2.56% of supply, and its controlling set is UNMEASURED, NOT EMPTY.** The BNB timelock `0xf0237a1cb794ea830129966049ef99243082ff0c` reads `getMinDelay()` = **172,800s = 2 days** (confirmed; the walk's figure holds) and is self-administered, with the BNB ProxyAdmin's `owner()` pointing at it and `EXECUTOR = address(0)` there too. **But I enumerated seven candidate holders for PROPOSER and CANCELLER and found NONE** — including Saturn, whose nonce on BNB is **0**, i.e. it has never transacted there. **That is an unread surface and I infer no absence from it**; the honest statement is that 2.56% of USDat sits behind a 2-day delay whose proposer is unidentified. usdat ethereum 67,164,376.55 / bnb 1,761,858.09.",
  "code_facts": [
   "SaturnTimelock 0xfd5782e3bff366601da3973ae30c583de4f08a67 (ethereum): getMinDelay 432000s = 5d; getThreshold REVERTS (it is a timelock, not a Safe); hasRole(DEFAULT_ADMIN, itself) true => SELF-ADMINISTERED. Re-measured by riskAnalyst 2026-09-10.",
   "⚠️ 0x610182581C93687Ca03F4a8E7f124f8cEC616820 is represented by Saturn as a FIREBLOCKS 2-of-3 MPC WALLET (issuer representation, pending independent attestation). On-chain reads cannot distinguish MPC from a single-key EOA — code_present false and a low nonce look identical either way. Read 'single key' framings as 'a 2-of-3 quorum internal to one operator'. The no-independent-veto finding is unaffected: the same quorum holds PROPOSER and CANCELLER.",
   "⚠️ PROPOSER = CANCELLER = 0x610182581C93687Ca03F4a8E7f124f8cEC616820, an EOA (no code, nonce 38). EXECUTOR false, DEFAULT_ADMIN false for it; 0x…dEaD false on all four roles; a FABRICATED role false. NO INDEPENDENT VETO.",
   "✅ hasRole(EXECUTOR, address(0)) = TRUE on both the ethereum 5d and the bnb 2d timelock => execution is PERMISSIONLESS once an operation matures.",
   "✅ USDat ProxyAdmin 0xcf1072da5f0d127aef99136489bad08bfa3d1a7d owner() = the 5d SaturnTimelock; BNB ProxyAdmin 0xcb8384df068b55228a66ea6efab4a10fcbfa42a2 owner() = the 2d BNB timelock. The proxy chain terminates at the timelock on both chains.",
   "✅ sUSDat vault 0xd166337499e176bbc38a1fbd113ab144e5bd2df7 and WithdrawalQueue 0x4bc9fec04f0f95e9b42a3ef18f3c96fb57923d2e: hasRole(DEFAULT_ADMIN, timelock) true, 0x…dEaD false, FABRICATED role false.",
   "⚠️ BNB timelock 0xf0237a1cb794ea830129966049ef99243082ff0c getMinDelay 172800s = 2d, self-administered. PROPOSER and CANCELLER holders NOT FOUND among 7 enumerated candidates — UNMEASURED, NOT ABSENT. Saturn's nonce on BNB is 0.",
   "⚠️ Exit-vs-notice UNRESOLVED: vault vestingPeriod() = 259200s (3d, a yield parameter); the withdrawal-queue delay did not resolve from 12 candidate signatures, with a fabricated getter reverting as control. WithdrawalQueue totalSupply() = 0 (nothing pending).",
   "sUSDat is 18-decimal while USDat is 6-decimal; totalAssets 73,385,123.07 USDat against 72,087,825.64 sUSDat => NAV ~1.018. Decimals read per token, not assumed.",
   "usdat supply: ethereum 67,164,376.55 (97.44%), bnb 1,761,858.09 (2.56%).",
   "⚠️ EXIT PATH NOT ESTABLISHED, and both published candidates are ruled out. vestingPeriod() = 259200s (3d) is an ERC-4626 YIELD-DRIP parameter (pairs with lastDistributionTimestamp and MAX_VESTING_PERIOD = 7776000s/90d), not a redemption queue. PegTracker's trust_stack.vesting_period_days = 30 is a HARDCODED LITERAL at saturn_backing_analyzer.py:1856 and 2592000s is returned by NO getter on the vault or the WithdrawalQueue across 18 candidate signatures (fabricated getter reverts as control)."
  ],
  "cross_axis": [
   "NOT priced here: the STRC/apxUSD look-through, the NAV discount and the cooldown's ECONOMIC cost are axis-4 facts. This axis prices who can act and how fast.",
   "⚠️ Anything reasoning from 'a 5-day timelock protects holders' must carry that the proposer is also the canceller AND that the withdrawal delay is unresolved, or it overstates the protection."
  ],
  "coverage_note": "⚠️ READER-FACING SCOPE LIMIT. 2 of 2 declared chains measured (ethereum, bnb). BNB PROPOSER/CANCELLER holders UNRESOLVED after enumerating 7 candidates — unread, not empty.",
  "provenance_note": "PRODUCER BOOKKEEPING. ⚠️ security_analyst's walk for this asset came from a codex run that EXITED 1 AFTER DELIVERING; they verified the file STRUCTURALLY ONLY and did not re-measure its on-chain claims, which rested on backing-monitor's seven-criteria check. riskAnalyst therefore re-measured every load-bearing claim independently on 2026-09-10: both timelock delays, self-administration, role membership with dEaD and fabricated-role controls, ProxyAdmin ownership, permissionless execution, and the proposer/canceller identity. ALL WALK CLAIMS CHECKED HERE WERE CONFIRMED. The proposer=canceller finding, the permissionless-execution finding and the BNB unmeasured-holder finding are riskAnalyst's and are not in the walk.",
  "coverage_summary": "2 of 2 declared chains measured (ethereum, bnb). BNB PROPOSER/CANCELLER holders UNRESOLVED after enumerating 7 candidates — unread, not empty.",
  "as_of": "2026-09-10T12:00:00Z"
 }
}