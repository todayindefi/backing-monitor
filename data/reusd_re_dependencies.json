{
  "schema_version": "dependencies/1",
  "producer": "riskanalyst",
  "asset": "reusd_re",
  "as_of": "2026-08-30T00:00:00Z",
  "dependencies": {
    "upstream": [
      {
        "name": "Re reinsurance book",
        "metric": "senior-tranche asset exposure",
        "source": "issuer disclosures",
        "note": "The product itself; concentration here is the design, not a defect. ⚠️ Off-chain reinsurance trusts and counterparties are NOT independently measurable from Ethereum state."
      },
      {
        "name": "Ethena sUSDe (collateral layer)",
        "link": "?asset=susde",
        "link_type": "internal",
        "metric": "93.87% of issuer-reported COMBINED protocol reserves, and the Ethereum redemption payout asset",
        "source": "issuer dashboard (via PegTracker)",
        "note": "⚠️ The percentage is COMBINED reUSD + reUSDe and must NOT be read as a reUSD-only allocation. Imports Ethena risk into a reinsurance product — a dependency a reader would not expect from the category."
      }
    ],
    "downstream_tracked": false,
    "note": "⚠️ DELIBERATE REDUCTION, not a loss. Two entries by design. The NAV oracle write path was carried here and has been removed: it is the issuer's OWN CONTROL SURFACE, not an external counterparty whose failure propagates in, and mixing the two kinds makes 'dependency' ambiguous on the axis a reader reaches looking for counterparties. It is rendered on AXIS 1, beside the NAV the discount is measured against, and on AXIS 5, in the authority walk beside the undelayed mint path. These entries are riskAnalyst-authored and CARRY PegTracker's measured figures rather than replacing them — a per-field array swap would otherwise drop the sUSDe measurement off the axis a reader checks for dependencies."
  }
}
